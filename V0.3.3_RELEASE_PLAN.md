# v0.3.3 Release & Housekeeping Plan — Pre-Tauri Closure
**Created:** 2026-08-25 · **Status:** ✅ DONE (2026-08-25) — Tauri planning unblocked
**Context:** harvest-honest core is DONE & PROVEN (`d562b86`). This plan
closes out the current web version properly before Tauri planning starts.
Ordered highest priority first.

---

## Phase 1 — Release closure · ✅ DONE

- [x] AGENTS.md header synced ("Last updated: 2026-08-25 (v0.3.3)" +
      HARVEST/session/tauri docs in planning-docs list).
- [x] `RELEASE_NOTES_v0.3.3.md` written.
- [x] Tagged `v0.3.3` (commit `c79c045`).

## Phase 2 — Hygiene · ✅ DONE

- [x] **middleware → proxy rename:** `src/middleware.ts` → `src/proxy.ts`,
      export `middleware()` → `proxy()`. Deprecation warning gone; rate
      limiting verified functional through the renamed proxy (burst test:
      exactly 120× HTTP 200 then 429s). No collision — no prior proxy.ts
      existed (Next 16 was reporting middleware.ts under the proxy name).
- [x] **`npm audit fix`:** 9 → 4 highs. Cleared brace-expansion (×2),
      js-yaml, deepmerge-ts. Remaining 4 = **sharp/libvips inherited CVEs**
      (fix = breaking sharp@0.35.3) — ACCEPTED/deferred: localhost image
      serving, low exposure; revisit at Tauri packaging when Next/image
      usage is reviewed.
- [x] Gates re-run after lockfile change: vitest 119/119 · tsc 0 · eslint
      0/0 · Playwright 16/16 (after fresh dev-server restart; the interim
      16-failures were the stale server against updated node_modules).
- Commit: `bfcf01f`.

## Phase 3 — Deferred / documented · LOW · no code

Recorded as accepted limitations (no action now):

- **Node ≥22.13 upgrade** — kills the EBADENGINE warning; system-level,
  user's machine decision. Verify gates after any upgrade.
- **Real webhook/SMTP delivery in production** — long-standing UNPROVEN;
  proven only against webhook.site/Ethereal. Desktop packaging does not
  change this, but revisit before charging clients for alerting.
- **GitHub Actions execution** — workflows verified by construction only;
  becomes REQUIRED during Tauri (per-OS build matrix) — folded into the
  Tauri plan rather than done twice here.
- **sharp@0.35.3 breaking upgrade** — clears the last 4 advisories; do it
  during Tauri packaging with a full visual pass of next/image output.

## Explicitly OUT of scope (already closed)

Harvest depth (platform-limited, honestly labeled), self-monitoring,
tenant-scoping sweep, rate limiting — all DONE & PROVEN; see
SESSION_SUMMARY_2026-08-25.md.

---

## After this plan

Tauri revival plan drafting is unblocked. Inputs ready:
`TAURI_COMPATIBILITY_RESEARCH_2026-08-25.md` (audit + framework decision +
stale-plan corrections) and the archived scaffold reference.
