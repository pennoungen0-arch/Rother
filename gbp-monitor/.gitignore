# GBP Monitor .gitignore
#
# Per GBP_MONITOR_PLAN.md Section 3 the raw_html/ directory is gitignored
# (raw captured pages are too large + may contain reviewer PII). The
# snapshots/ and reviews_new/ directories ARE committed (they're the
# dashboard's data source) — see .github/workflows/scrape.yml
# which `git add`s them.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Playwright
playwright-report/

# Captured raw HTML (per Section 3 — large + potentially PII)
data/raw_html/

# Verification evidence (screenshots + captured HTML — large, transient, PII)
data/verify/

# Local-only Python version manager files
.python-version

# Runtime logs (large, transient)
data/run.log

# Persisted Google cookie jar (NID warm-up, M8) — real cookies, never commit
data/storage_state.json

# Backup archives (never commit)
data/backups/

# Editor
.vscode/
.idea/
*.swp
