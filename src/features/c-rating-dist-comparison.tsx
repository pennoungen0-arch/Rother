"use client";

import { CompetitorRatingDistComparison } from "@/components/dashboard/competitor-rating-dist-comparison";

export default function RatingDistComparisonFeature() {
  return <CompetitorRatingDistComparison refreshKey={0} />;
}
