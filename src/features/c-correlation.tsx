"use client";

import { CompetitorCorrelation } from "@/components/dashboard/competitor-correlation";

export default function CorrelationFeature() {
  return <CompetitorCorrelation refreshKey={0} />;
}
